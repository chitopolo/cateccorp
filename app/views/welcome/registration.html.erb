<div class = "hero-unit">
			<div id="user_nav">
              <% if current_user %>
                Iniciaste sesion como <strong><%= current_user.name %></strong>!
                <%= link_to "Cerrar sesion", signout_path, id: "sign_out" %>
              <% else %>
                <%= link_to "Inicia sesion con tu Facebook", "/auth/facebook", id: "sign_in" %>
              <% end %>
            </div>

            <div class ="btn "><%= link_to "Registro Sin Facebook", :controller => "attendees", :action => "new" %>
            </div>

</div>

<% if current_user%>
            <div class ="btn btn-primary"><%= link_to "Continuar", :controller => "attendees", :action => "new" %>
            </div>
<% else %>
<% end -%>
